import React from 'react';
import ReactDOM from 'react-dom/client';
import App from '../samvaad_multilingual_education_translation_app';
import './index.css';

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
);
